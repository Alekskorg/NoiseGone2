
3. Открыть [http://localhost:3000](http://localhost:3000)

---

## ⚡ Деплой на Vercel

1. Залей код в репозиторий GitHub.
2. Импортируй репозиторий на [vercel.com](https://vercel.com/new) (логин через GitHub).
3. Проверь настройки (Node.js 20.x, никаких билд-скриптов не нужно).
4. После деплоя открой сайт → загрузи файл (mp3/wav/m4a, до 50MB и до 5 минут) → получи готовый mp3.

---

## 💡 Особенности

- Одна страница, две локали (RU/EN).
- Presets для разных сценариев (Podcast, Interview, Voice-over, Field).
- Прогресс загрузки и обработки.
- Видна причина ошибки (таймаут, 413, 415 и др.).
- Никаких внешних CDN.
- Всё работает на бесплатном слоте Vercel (но может быть лимит времени ~60 сек).

---

## ❗️ Ошибки

- **ENOENT:** если не находит ffmpeg — проверь, что есть `ffmpeg-static` в node_modules и `includeFiles` в `vercel.json`.
- **Timeout:** превышено время обработки (сложные/большие файлы). Обрезайте/уменьшайте файл.
- **413/415:** файл слишком большой или неправильный формат/длительность.
- **500:** внутренние ошибки сервера (смотри server logs в Vercel).

---

## 🔐 Лимиты

- Бесплатно — файлы до 5 минут и до 50МБ.
- Ограничение по количеству файлов — только через UI/заглушку, биллинга нет.

---

**Вопросы/ошибки:**  
Пиши issues в репозитории или мне в Telegram.
